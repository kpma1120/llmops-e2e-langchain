# ================================
# Credentials and API Keys
# ================================

# Path to your Google Cloud service account JSON file
# Example: credentials/service-account.json
GOOGLE_APPLICATION_CREDENTIALS=credentials/your-service-account.json

# Tavily API Key (for web crawling)
TAVILY_API_KEY=your-tavily-api-key

# Pinecone API Key (only required if VECTORSTORE_PROVIDER=pinecone)
PINECONE_API_KEY=your-pinecone-api-key


# ================================
# Vector Store Configuration
# ================================

# Choose a vector store backend: "chroma" or "pinecone"
VECTORSTORE_PROVIDER=chroma


# ================================
# Pinecone Parameters
# ================================

# Pinecone index name (only used when VECTORSTORE_PROVIDER=pinecone)
PINECONE_INDEX_NAME=your-pinecone-index-name


# ================================
# Chroma Parameters
# ================================

# Directory where ChromaDB will persist its data
CHROMA_PERSIST_DIR=./chroma_db
